{
  "name": "subnetpoll",
  "version": "1.0.0",
  "private": true,
  "description": "Static Bittensor subnet poll dashboard.",
  "scripts": {
    "check": "node --check app.js && node --check scripts/fetch-reactions.mjs && node --check scripts/check-secrets.mjs && node scripts/check-secrets.mjs",
    "check:secrets": "node scripts/check-secrets.mjs"
  }
}
