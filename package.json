{
  "name": "thumbsflow",
  "version": "1.0.0",
  "private": true,
  "description": "Static Bittensor subnet poll dashboard.",
  "scripts": {
    "refresh:market": "python3 scripts/fetch-market-data.py",
    "check": "node --check app.js && node --check data.js && node --check market-data.js && node --check scripts/fetch-reactions.mjs && node --check scripts/check-secrets.mjs && node scripts/check-secrets.mjs",
    "check:secrets": "node scripts/check-secrets.mjs"
  }
}
